import React from "react";
import { Companions as CompanionsStyle } from "./ArchiveStyle";

const Companions = ({ onClick }) => {
  return <CompanionsStyle onClick={onClick}>Companions</CompanionsStyle>;
};

export default Companions;
