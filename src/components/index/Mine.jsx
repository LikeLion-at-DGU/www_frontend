import { MineStyle } from "./BtnStyle";

const Mine = (props) => {
  return (
    <MineStyle
      handlewidth={props.handlewidth}
      handleheight={props.handleheight}
    >
      mine
    </MineStyle>
  );
};

export default Mine;
